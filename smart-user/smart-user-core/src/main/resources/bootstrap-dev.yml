spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  redis:
    database: 0
    host: 124.223.80.135
    port: 6379
    password: ak87715700

  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart_user?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root


# 日志配置
logging:
  config: classpath:log4j2.xml


tlog:
  enable-invoke-time-print: true